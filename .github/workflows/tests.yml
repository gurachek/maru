name: tests

on:
  push:
    branches: [main]
  pull_request:

jobs:
  hooks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Hook test harness (pure sh, jq preinstalled on the runner)
        run: sh tests/hooks_test.sh
